<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../Banter.Protocol/Banter.Protocol.csproj" />
    <!-- 0.1.0-alpha.5.local: a local build, restored through this project's own NuGet.config.
         Switch to 0.1.0-alpha.5 and delete that file once the feed has it. -->
    <PackageReference Include="CupriNet.Nodestar" Version="0.1.0-alpha.5.local" />
  </ItemGroup>
</Project>
