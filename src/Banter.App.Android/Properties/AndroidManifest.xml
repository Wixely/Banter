<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Talking to the server. RECORD_AUDIO is here for the voice work that follows; Android also
       requires it to be requested at runtime, which the head does not do yet. -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <application android:label="Banter" android:supportsRtl="true" />
</manifest>
